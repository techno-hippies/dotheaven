{
  "name": "content-feed-subgraph",
  "version": "1.0.0",
  "description": "Content registry subgraph on MegaETH — dotheaven",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build"
  },
  "keywords": ["the-graph", "megaeth", "filecoin", "content", "music"],
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
