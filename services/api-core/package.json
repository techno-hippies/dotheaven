{
  "name": "api-core-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ${API_CORE_D1_DATABASE:-${D1_DATABASE:-api-core}} --local --file=./schema.sql && bun run scripts/db-migrate.ts --mark-all-applied",
    "db:init:remote": "wrangler d1 execute ${API_CORE_D1_DATABASE:-${D1_DATABASE:-api-core}} --remote --file=./schema.sql && bun run scripts/db-migrate.ts --remote --mark-all-applied",
    "db:migrate": "bun run scripts/db-migrate.ts",
    "db:migrate:remote": "bun run scripts/db-migrate.ts --remote",
    "seed": "bun run scripts/seed.ts && wrangler d1 execute ${API_CORE_D1_DATABASE:-${D1_DATABASE:-api-core}} --local --file=./scripts/seed.sql",
    "seed:remote": "bun run scripts/seed.ts && wrangler d1 execute ${API_CORE_D1_DATABASE:-${D1_DATABASE:-api-core}} --remote --file=./scripts/seed.sql",
    "check": "tsc --noEmit",
    "test:unit": "bun test tests/*.test.ts",
    "test:smoke": "bun --env-file=.dev.vars run tests/smoke/music-flow.smoke.ts",
    "test:music": "bun --env-file=.dev.vars run tests/smoke/music-flow.smoke.ts",
    "test:study-sets": "bun --env-file=.dev.vars run tests/smoke/study-sets.smoke.ts",
    "test:music:finalize": "bun test tests/music-finalize.test.ts"
  },
  "dependencies": {
    "ethers": "6.16.0",
    "hono": "^4.4.0",
    "opentype.js": "1.3.4",
    "svg2png-wasm": "1.4.1",
    "viem": "2.44.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240524.0",
    "@types/opentype.js": "1.3.8",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
