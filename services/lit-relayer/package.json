{
  "name": "@heaven/lit-relayer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p api && bun build src/mint-user-pkp.ts --outfile=api/mint-user-pkp.js --target=node --format=cjs && bun build src/health.ts --outfile=api/health.js --target=node --format=cjs",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@lit-protocol/auth": "8.2.3",
    "@lit-protocol/lit-client": "8.3.1",
    "@lit-protocol/networks": "8.4.1",
    "viem": "2.38.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.6.0",
    "vercel": "^37.0.0"
  }
}
