# Web New

Fresh SolidJS + Vite + Bun + TypeScript app scaffold with Storybook.

## Commands

Run from repo root:

- `bun run --cwd apps/web-new dev`
- `bun run --cwd apps/web-new check`
- `bun run --cwd apps/web-new storybook`
